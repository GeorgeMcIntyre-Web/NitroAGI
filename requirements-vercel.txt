# Vercel-optimized requirements for NitroAGI NEXUS
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
redis==5.0.1
asyncpg==0.29.0
httpx==0.25.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.6
bcrypt==4.0.1
passlib==1.7.4

# Core dependencies
pyyaml==6.0.1
python-dotenv==1.0.0
structlog==23.2.0
rich==13.7.0
typer==0.9.0

# AI/ML (lightweight versions for serverless)
openai==1.3.5
anthropic==0.7.8

# Async support
asyncio-mqtt==0.16.1
aiofiles==23.2.1

# Monitoring
psutil==5.9.6

# Remove heavy dependencies that don't work well in serverless:
# - torch (too large)
# - transformers (too large) 
# - huggingface-hub (will use API instead)